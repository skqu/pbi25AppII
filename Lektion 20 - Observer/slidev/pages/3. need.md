

# Behov


```mermaid
sequenceDiagram
    Order ->> Ui : ChangeLayout(layout)
    Ui -->> Order : status
    Order ->> Log : WriteLog("Order change")
    Log -->> Order : status
    Order ->> Mail : WriteMail(content)
    Mail -->> Order : status
    Order ->> Stock : UpdateStock(item)
    Stock -->> Order : status
```