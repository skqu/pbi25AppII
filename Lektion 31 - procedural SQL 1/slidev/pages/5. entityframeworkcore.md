

# Procedure

<v-clicks at="1">

- dotnet ef add migration sp-CreateLoan 
- Write the precedure in the function up
- dotnet ef database update

</v-clicks>

````md magic-move {at: 1, lines: true}
```ts
```

```ts
public partial class spCreateLoan : Migration
{
    protected override void Up(MigrationBuilder migrationBuilder)
    {
    }

    protected override void Down(MigrationBuilder migrationBuilder)
    {
    }
}

```

```ts
public partial class spCreateLoan : Migration
{
    protected override void Up(MigrationBuilder migrationBuilder)
    {
        var sp = @"
        DROP PROCEDURE IF EXISTS CreateLoan;

        CREATE PROCEDURE CreateLoan(
            IN p_UserId INT,
            IN p_BookId INT,
            IN p_LoanDate DATE,
            OUT p_LoanCount INT
        )
        BEGIN
            -- Insert loan
            INSERT INTO Loan (UserId, BookId, LoanDate)
            VALUES (p_UserId, p_BookId, p_LoanDate);

            -- Return number of loans for user
            SELECT COUNT(*)
            INTO p_LoanCount
            FROM Loan
            WHERE UserId = p_UserId;
        END;
        ";

        migrationBuilder.Sql(sp);
    }

    protected override void Down(MigrationBuilder migrationBuilder)
    {
    }
}
```

```ts
CALL CreateLoan(1, 10, '2026-04-22', @count);
SELECT @count;
```
````