


# Entity Framework Core 
For [DbSet](https://learn.microsoft.com/en-us/dotnet/api/microsoft.entityframeworkcore.dbset-1?view=efcore-10.0) operationer

::left::

```ts{1,14|3-7,18-21|11}
    public class UserContext : DbContext
    {
        public UserContext
        (
            DbContextOptions<UserContext> 
            options
        ) : base(options)
        {
        }

        public DbSet<UserModel> Users { get; set; }
    }

    static void Main()
    {
        ... 
        
            builder.Services.AddDbContext<UserContext>
            (
                options =>
                options.UseInMemoryDatabase("users.db"));
        ...
    }
```

::right::


```ts{0|8|8,9|14-15}
        public UserRepository(UserContext userContext)
        {
            _userContext = userContext;
        }

        public void Add(UserModel model)
        {
            _userContext.Users.Add(model);
            _userContext.SaveChanges();
        }

        public UserModel? GetById(byte modelId)
        {
            UserModel? user = _userContext.Users.
            FirstOrDefault(u => u.Id == modelId);

            if (user == null)
            {
                Console.WriteLine("User not found");
            }

            return user;
        }


```