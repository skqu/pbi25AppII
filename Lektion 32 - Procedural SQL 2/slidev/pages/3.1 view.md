

# Views EF Core

````md magic-move {at: 1, lines: true}
```ts
    dotnet ef migrations add view_UserLoans
    code .
```

```ts
    // *_view_UserLoans.cs
    public partial class view_UserLoans : Migration
    {
        /// <inheritdoc />
        protected override void Up(MigrationBuilder migrationBuilder)
        {
            var sp = @"
            DROP VIEW IF EXISTS UserLoans;
            CREATE VIEW UserLoans AS
                SELECT 
                    l.BookId,
                    l.CopyNumber,
                    u.Name, 
                    l.UserId
                FROM Loans l
                JOIN Users u ON l.UserId = u.UserId;
            ";

            migrationBuilder.Sql(sp);

        }

        /// <inheritdoc />
        protected override void Down(MigrationBuilder migrationBuilder)
        {
            migrationBuilder.Sql(@"DROP VIEW IF EXISTS UserLoans;");
        }
    }
```

```ts
    // UserLoansViewModel.cs
    public class UserLoansViewModel
    {
        public int BookId {set; get;}
        public int CopyNumber {set; get;}
        public string Name {set; get;}
        public int UserId {get; set;}
    }
```

```ts
    // LibraryContext.cs

    public class LibraryContext : DbContext
    {

        // Same DBSet as before
        public DbSet<UserLoanViewModel> UserLoans { get; set; }

        public LibraryContext(DbContextOptions<LibraryContext> options) : base(options)
        {
        }

        protected override void OnModelCreating(ModelBuilder modelBuilder)
        {
            // Same implemenation as before

            // View for User Loans
            modelBuilder.Entity<UserLoanViewModel>(entity =>
                {
                    entity.HasNoKey();
                    entity.ToView("UserLoans");
                });
        }
    }
```

```ts
    // LoanRepository.cs
    public List<UserLoanViewModel> GetLoansByUser(int userId)
    {
        return _context.UserLoans
            .Where(l => l.UserId == userId)
            .ToList();
    }

```

```ts
dotnet ef database update
```


````