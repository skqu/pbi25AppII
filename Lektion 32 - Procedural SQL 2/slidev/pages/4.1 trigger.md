

# Trigger EF Core

````md magic-move {at: 1, lines: true}
```ts
    dotnet ef migrations add trigger_PreventDoubleLoan
    code .
```


```ts
    // *trigger_PreventDoubleLoan.cs
    public partial class trigger_PreventDoubleLoan : Migration
    {
        /// <inheritdoc />
        protected override void Up(MigrationBuilder migrationBuilder)
        {
            var sp = @"
            DROP TRIGGER IF EXISTS PreventDoubleLoan;
            CREATE TRIGGER PreventDoubleLoan
            BEFORE INSERT ON Loans
            FOR EACH ROW
            BEGIN
            IF EXISTS (
                SELECT 1 FROM Loans
                WHERE BookId = NEW.BookId
                AND CopyNumber = NEW.CopyNumber
            ) THEN
                SIGNAL SQLSTATE '45000'
                SET MESSAGE_TEXT = 'Book copy is already loaned';
            END IF;
            END;
            ";

            migrationBuilder.Sql(sp);

        }

        /// <inheritdoc />
        protected override void Down(MigrationBuilder migrationBuilder)
        {
            migrationBuilder.Sql(@"DROP TRIGGER IF EXISTS PreventDoubleLoan;");
        }
    }
```

```ts
    // UserService.cs
    public string LoanBook(LoanRequestDto loanDto)
    {
        // Same implementation but change CreateLoan logic. 
        try
        {
            int loanCount = _loansRepo.CreateLoan(loanModel);

            Console.WriteLine($"Loan created. User now has {loanCount} loans.");
            return loanCount.ToString();
        }
        catch (MySqlConnector.MySqlException ex)
        {
            Console.WriteLine("Database rejected the loan:");
            Console.WriteLine(ex.Message);
            return ex.Message;
        }
    }
```


```ts
dotnet ef database update
```


````