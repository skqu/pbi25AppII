

# Behovet

::left::

## Løsningsforslag


```mermaid{scale:0.6}
sequenceDiagram
    participant Client
    participant System
    participant NewPayment

    Client ->> System : Pay(orderId, amount)
    System ->> NewPayment : CreateTransaction(user, value, currency)
    NewPayment -->> System : PaymentStatus
    System -->> Client : Result
```


::right::

## Problemet
- Du må ikke ændre eksternt system
- Du vil ikke ændre hele dit system
- Du skal få dem til at tale sammen

<v-click>

## Behov
Vi ønsker:
- At oversætte mellem to interfaces
- Uden at ændre eksisterende kode
- Uden at sprede tilpasningskode overalt
</v-click>